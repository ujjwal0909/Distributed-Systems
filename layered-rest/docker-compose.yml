version: '3.8'
services:
  node:
    build: ./node
    ports:
      - "8000-8004:8000"
    environment:
      - PEER_NODES=http://node:8000,http://node:8001,http://node:8002,http://node:8003,http://node:8004
    deploy:
      replicas: 5
    restart: unless-stopped
